# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "harness-ultragoal-plugin-proposal"

[setup]
script = '''
cd "${CODEX_WORKTREE_PATH:-$PWD}"
bash "$HOME/.codex/bin/codex-worktree-env"
'''

[cleanup]
script = '''
cd "${CODEX_WORKTREE_PATH:-$PWD}"
if [ -d .codex-worktree ]; then
  receipt="${TMPDIR:-/tmp}/codex-worktree-cleanup-$(basename "$PWD")-$(date -u +%Y%m%dT%H%M%SZ).txt"
  {
    printf 'CODEX_WORKTREE_ROOT=%s\n' "$PWD"
    printf 'cleanup_target=.codex-worktree\n'
    find .codex-worktree -type f | sort
  } > "$receipt"
  rm -rf .codex-worktree
  printf 'cleanup_receipt=%s\n' "$receipt"
fi
'''

[[actions]]
name = "Env"
icon = "tool"
script = '''
cd "${CODEX_WORKTREE_PATH:-$PWD}"
bash "$HOME/.codex/bin/codex-worktree-env"
. .codex-worktree/env.sh
env | sort | grep -E '^(CODEX_WORKTREE_|CARGO_TARGET_DIR|CARGO_LLVM_COV_TARGET_DIR|TMPDIR|TMP=|TEMP=|HOST=|HARNESS_ULTRAGOAL_)'
'''

[[actions]]
name = "Agent Docs"
icon = "tool"
script = '''
cd "${CODEX_WORKTREE_PATH:-$PWD}"
. .codex-worktree/env.sh 2>/dev/null || bash "$HOME/.codex/bin/codex-worktree-env"
. .codex-worktree/env.sh
test -f docs/agent-first-repo-shape.md
test -f templates/AGENT_STANDARDS.md
test -f skills/agent-first-repo-init/SKILL.md
test -f skills/agent-first-repo-retrofit/SKILL.md
'''

[[actions]]
name = "Fast Check"
icon = "tool"
script = '''
cd "${CODEX_WORKTREE_PATH:-$PWD}"
. .codex-worktree/env.sh 2>/dev/null || bash "$HOME/.codex/bin/codex-worktree-env"
. .codex-worktree/env.sh
bash -n .codex/setup-worktree-env.sh
cargo test --offline
'''

[[actions]]
name = "Git State"
icon = "tool"
script = '''
cd "${CODEX_WORKTREE_PATH:-$PWD}"
git status --short || true
git log -1 --oneline || true
'''
