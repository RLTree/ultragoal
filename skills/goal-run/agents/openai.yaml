interface:
  display_name: "Goal Run"
  short_description: "Orchestrate durable dependency-closed work"
  default_prompt: "Use $harness-ultragoal:goal-run to orchestrate this durable goal with disjoint leases and honest stop conditions."
policy:
  allow_implicit_invocation: false
