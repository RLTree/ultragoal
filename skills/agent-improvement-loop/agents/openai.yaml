interface:
  display_name: "Agent Improvement Loop (Legacy Alias)"
  short_description: "Route this legacy alias to its canonical skill"
  default_prompt: "Use $harness-ultragoal:agent-improvement-loop to preserve and route my unchanged request through its canonical skill."
policy:
  allow_implicit_invocation: false
