interface:
  display_name: "Repository Fit"
  short_description: "Fit fresh and existing repositories safely"
  default_prompt: "Use $harness-ultragoal:repository-fit to inspect this repository and produce a safe fit plan before any authorized change."
policy:
  allow_implicit_invocation: false
