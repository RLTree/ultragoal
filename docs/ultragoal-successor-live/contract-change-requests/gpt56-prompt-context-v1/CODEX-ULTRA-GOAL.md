/goal Complete the live Harness Ultragoal product under the adopted successor contract.

Use the verified handoff only to establish contract provenance. Operate on the actual live Git repository and recompute repository, candidate, tools, permissions, installed product surfaces, and exposed runtime capabilities before planning or claiming evidence. Treat snapshots, chat, memory, prompts, tests, receipts, and generated artifacts only at the truth layer they actually prove. Record model, mode, reasoning, or orchestration configuration only when the runtime exposes it.

Load the stable laws, dependency graph, model-adaptive prompt/context contract, and current live control state first. Use the adopted context-routing map to load only the exact requirements, surfaces, tools, claims, migrations, decisions, and evidence needed for active nodes; load complete registries only at a global reconciliation boundary.

The Ultra root alone owns adopted manifests, shared schemas and dependencies, public command and generated authority, integration, migration authority, claim decisions, release state, and final reporting. Run substantial dependency-closed node packages concurrently when their ownership is disjoint, with no more than one implementation lane per node. Give every worker an explicit run-scoped lease and require `WorkerResult-v1`, independent review, and root acceptance. Keep reviewer roles concise and bind each invocation to an exact task envelope. Stop a rolling review after the first decisive material defect; reserve comprehensive full-scope review for its named integrated claim boundary.

Complete both the plugin product and its typed Rust enforcement kernel. Implement required Harness-owned tools before dependent behavior, route or retire duplicate authority under the migration contract, preserve zero-write read/help/parse/query behavior, and verify representative clean, dirty, conflict, failure, recovery, interruption, repeat-use, installation, discovery, runtime, security, mutation, and false-pass journeys.

Continue through ordinary blockers by repairing or replanning legal work and advancing independent nodes. Stop only for unavailable required access, external authority with no safe default, or a destructive decision. Report only accepted sets, material `REWORK`, root integrations, changed candidate identity or claim ceilings, node closures, and genuine blockers.

Finish only when every N00-N17 completion surface has current candidate-bound evidence, all material findings and decisions are reconciled, duplicate authority is absent, representative product behavior passes, and the final integrated candidate survives independent falsification. Otherwise keep the goal active and report the highest honest ceiling and exact next action.
