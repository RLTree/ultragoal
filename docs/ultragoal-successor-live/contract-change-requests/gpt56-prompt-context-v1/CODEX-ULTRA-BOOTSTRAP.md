# Codex Ultra Bootstrap

Status: proposed replacement; root adoption required

## Verify provenance

Locate the handoff by `FINAL-HANDOFF-MANIFEST.sha256` and verify every listed digest before using it. Reject missing, altered, or unexplained contract files. The handoff proves only its own bytes and provenance; it does not prove the live repository, installation, discovery, runtime, product behavior, release, or completion.

## Open live source

Open the actual Harness Ultragoal Git working tree intended for implementation. Recompute the canonical root, branch and HEAD, status, worktrees, candidate identity, submodules, dirty and untracked risks, installed tools, Cargo metadata when available, permissions, host capabilities, plugin/marketplace/skill/agent surfaces, CLI commands, generated authorities, and exposed runtime metadata. Resolve discrepancies in favor of current live evidence.

Do not modify the adopted contract silently. When current evidence or reviewed research requires a normative change, prepare an exact contract change request. The Ultra root alone reviews and adopts it, updates manifests and digests, and reopens invalidated evidence.

## Load startup authority

Read this minimal startup set:

1. `FINAL-CONTRACT/00-READ-ME-FIRST.md`;
2. `FINAL-CONTRACT/CONTRACT_MANIFEST.json` metadata and contract-entry rows;
3. `FINAL-CONTRACT/01-AUTHORITY-AND-PRODUCT-LAWS.md`;
4. `FINAL-CONTRACT/MODEL-ADAPTIVE-PROMPT-AND-CONTEXT-CONTRACT.md`;
5. root-authority and active-node rows from `FINAL-CONTRACT/IMPLEMENTATION_DEPENDENCY_GRAPH.json`;
6. unresolved `FINAL-CONTRACT/OPEN-DECISIONS.md` rows affecting active nodes;
7. the current live critical-path board, accepted evidence, open findings, and active WorkerResults.

Do not load every registry by default. Use `FINAL-CONTRACT/CONTEXT-ROUTING-MAP.json` to select exact rows for each active node. Expand claim prerequisites and tool dependencies recursively. Unknown, duplicate, ambiguous, or conflicting stable IDs are blockers. Load complete registries only for global reconciliation, retirement, release, or completion when the invariant requires them.

## Configure work

Use the exposed runtime configuration that fits each task shape. Never infer Luna, Terra, Sol, Ultra, Max, Pro, reasoning effort, subagent support, or permissions from this document. Keep one stable outcome contract and change runtime configuration more often than prompt wording.

The root selects substantial dependency-closed packages and maintains at most one implementation lane per node. A lease must be disjoint in paths, semantic symbols, generated outputs, fixtures, and external effects. It names required positive, negative, race, mutation, security, and false-pass tests. Workers return `WorkerResult-v1`, request rather than apply root-only changes, and make no readiness, release, or completion claim.

Static custom-agent manifests contain only stable role and least-privilege boundaries. Every invocation receives an exact `AgentTaskEnvelope-v1`. Source reviewers remain read-only. If independent reproduction needs build or temporary-repository writes, use a separate ephemeral scratch-only verification lease; never grant candidate-source write authority merely to run tests.

Review frozen artifact sets continuously. Return `REWORK` after one decisive material defect and issue a corrective lease. Integrate independently accepted work promptly. Run comprehensive multi-role review only at the exact claim boundary that requires it.

## Claim discipline

Source, package, installed bytes, host discovery, runtime, product behavior, quality in use, release, and completion remain separate. Files, schemas, tests, receipts, telemetry, signatures, provenance, worker output, and goal state never substitute for named behavior. Read, help, version, parse, inspect, next, and query paths have zero hidden writes.

Unavailable capabilities lower only dependent claims. Continue legal independent work. Stop for unavailable required access, external authority with no safe default, or destructive decisions, preserving state and reporting the highest honest ceiling and exact next action.

Use the exact adopted `CODEX-ULTRA-GOAL.md` as the goal. Its byte identity must match the adopted manifest; do not reconstruct it from chat.
