/goal Complete the live Harness Ultragoal product under the verified successor contract. First locate this handoff bundle by `FINAL-HANDOFF-MANIFEST.sha256`, verify every listed digest, then read `FINAL-CONTRACT/00-READ-ME-FIRST.md`, `FINAL-CONTRACT/CONTRACT_MANIFEST.json`, `FINAL-CONTRACT/ULTRA-INPUT-MANIFEST.json`, `FINAL-CONTRACT/IMPLEMENTATION_DEPENDENCY_GRAPH.json`, `FINAL-CONTRACT/REQUIREMENT_TRACE.json`, and `FINAL-CONTRACT/CLAIM_REGISTRY.json`. Treat the supplied ZIP, snapshot findings, and this review as context—not live proof—and do not rely on hidden context from the Chat that produced them.

Open and operate on the actual live Harness Ultragoal Git repository, not the archived snapshot or handoff bundle. Before planning or writing, recompute the live repository root, branch/HEAD, status, worktrees, candidate identity, installed tools, supported Codex/plugin capabilities, permissions, and current command/component inventory. Record model, mode, reasoning, and runtime configuration only when Codex exposes that metadata; never infer it from this prompt. Lower affected claim ceilings when requested configuration or proof surfaces cannot be confirmed.

You are the Ultra root and sole owner of shared authority, adopted manifests, workspace dependency changes, public command catalog, global generated authority, integration, merge/rebase, migration registry, claim decisions, release state, and final reporting. Use read-only subagents for repository reconnaissance, current primary-source verification, product-journey review, claim falsification, security review, and orchestration/recovery review. Give implementation workers explicit run-scoped leases that are disjoint across paths, semantic symbols, generated outputs, fixtures, and external effects. Require `WorkerResult-v1` outputs, independent review, and root acceptance/reconciliation; workers may request but never apply root-only changes or claim completion.

Execute the dependency graph adaptively. Implement every missing Harness-owned custom tool before any downstream claim that depends on it. Balance completion of the plugin product—supported manifest and marketplaces, canonical skills, current custom-agent discovery, installation/discovery, fresh setup, retrofit, routine use, diagnosis, observability, and quality-in-use journeys—with the typed Rust CLI enforcement/acceleration kernel. Retire or route replaced lanes, gates, commands, tools, helpers, aliases, packages, schemas, state stores, receipts, agents, skills, and generated surfaces under `MIGRATION-AND-RETIREMENT.md`; no duplicate authority may remain.

Keep routine dirty-tree iteration conservative and fast. Reserve strict work for the exact dependency-closed claim boundary. Tests, receipts, generated rows, documentation, telemetry, signatures, provenance, or green commands never substitute for the named product behavior. Require real representative product verification, false-pass controls, independent reconciliation, and candidate-bound evidence. Read/help/parse/query paths must have zero hidden writes.

Continue through ordinary blockers by repairing or replanning legal dependency-closed work and by advancing independent nodes. Stop only for genuine external authority, unavailable required access, or destructive-action decisions, and then report the highest honest ceiling, preserved state, exact blocker, and exact next action. Do not declare readiness, release, or goal completion until their exact live proof surfaces pass and all material findings and required decisions are reconciled.
