# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "agent-first-repo"

[setup]
script = '''
cd "${CODEX_WORKTREE_PATH:-$PWD}"
/usr/bin/env -i \
  CODEX_WORKTREE_PATH="${CODEX_WORKTREE_PATH:-$PWD}" \
  PWD="$PWD" \
  CODEX_HOST_CARGO_BIN="$HOME/.cargo/bin/cargo" \
  PATH="/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin" \
  /bin/bash .codex/setup-worktree-env.sh
'''

[cleanup]
script = '''
/usr/bin/env -i \
  CODEX_WORKTREE_PATH="${CODEX_WORKTREE_PATH:-$PWD}" \
  PWD="$PWD" \
  PATH="/usr/bin:/bin" \
  TMPDIR="/tmp" \
  /bin/bash -s <<'CLEANUP'
set -euo pipefail
umask 077
cd "$CODEX_WORKTREE_PATH"
if [ -L .codex-worktree ] || { [ -e .codex-worktree ] && [ ! -d .codex-worktree ]; }; then
  echo 'refusing untrusted .codex-worktree cleanup path' >&2
  exit 1
fi
if [ -d .codex-worktree ]; then
  receipt="$(mktemp /tmp/codex-worktree-cleanup.XXXXXX.txt)"
  {
    printf 'CODEX_WORKTREE_ROOT=%s\n' "$PWD"
    printf 'cleanup_target=.codex-worktree\n'
    find .codex-worktree -type f | sort
  } > "$receipt"
  rm -rf .codex-worktree
  printf 'cleanup_receipt=%s\n' "$receipt"
fi
CLEANUP
'''

[[actions]]
name = "Env"
icon = "tool"
script = '''
cd "${CODEX_WORKTREE_PATH:-$PWD}"
/usr/bin/env -i CODEX_WORKTREE_PATH="${CODEX_WORKTREE_PATH:-$PWD}" PWD="$PWD" CODEX_HOST_CARGO_BIN="$HOME/.cargo/bin/cargo" PATH="/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin" /bin/bash .codex/setup-worktree-env.sh
.codex-worktree/run-command sh -c 'env | sort | grep -E "^(CODEX_WORKTREE_|CARGO_TARGET_DIR|CARGO_LLVM_COV_TARGET_DIR|TMPDIR|TMP=|TEMP=|HOST=|HARNESS_ULTRAGOAL_)"'
'''

[[actions]]
name = "Agent Docs"
icon = "tool"
script = '''
cd "${CODEX_WORKTREE_PATH:-$PWD}"
/usr/bin/env -i CODEX_WORKTREE_PATH="${CODEX_WORKTREE_PATH:-$PWD}" PWD="$PWD" CODEX_HOST_CARGO_BIN="$HOME/.cargo/bin/cargo" PATH="/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin" /bin/bash .codex/setup-worktree-env.sh
.codex-worktree/run-command sh -c 'test -f AGENTS.md && test -f AGENT_STANDARDS.md && test -f PLANS.md'
'''

[[actions]]
name = "Fast Check"
icon = "tool"
script = '''
cd "${CODEX_WORKTREE_PATH:-$PWD}"
/usr/bin/env -i CODEX_WORKTREE_PATH="${CODEX_WORKTREE_PATH:-$PWD}" PWD="$PWD" CODEX_HOST_CARGO_BIN="$HOME/.cargo/bin/cargo" PATH="/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin" /bin/bash .codex/setup-worktree-env.sh
.codex-worktree/run-command sh -c 'test -x scripts/check && scripts/check'
'''

[[actions]]
name = "Git State"
icon = "tool"
script = '''
cd "${CODEX_WORKTREE_PATH:-$PWD}"
/usr/bin/env -i CODEX_WORKTREE_PATH="${CODEX_WORKTREE_PATH:-$PWD}" PWD="$PWD" CODEX_HOST_CARGO_BIN="$HOME/.cargo/bin/cargo" PATH="/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin" /bin/bash .codex/setup-worktree-env.sh
.codex-worktree/run-command sh -c 'git status --short || true; git log -1 --oneline || true'
'''
