{
  "schema_version": "RepositoryFitApplyMediationFixtureCatalog-v1",
  "temporary_root": "/tmp/hul-repository-fit-apply-mediation-058",
  "claim_effect": "none",
  "production_permit_issuer": "absent",
  "public_apply_dispatch": "absent",
  "cases": [
    {"id": "fresh-repository-exact-apply", "class": "positive", "expect": "one permitted exact apply followed by byte, mode, object, protected-tree, and context reconciliation"},
    {"id": "partial-retrofit-exact-apply", "class": "positive", "expect": "only accepted missing or managed-outdated rows mutate"},
    {"id": "dirty-repository-preserved", "class": "positive", "expect": "unrelated preexisting dirty state is byte-for-byte and metadata-for-metadata preserved"},
    {"id": "already-fitted-idempotent-apply", "class": "positive", "expect": "a separately prepared zero-mutation request settles idempotently"},
    {"id": "matching-bytes-mode-only-apply", "class": "positive", "expect": "authoritative mode participates in the accepted mutation and terminal proof"},
    {"id": "managed-ancestor-exact-creation", "class": "positive", "expect": "every initially missing strict target ancestor is created as a stable root-owned directory with exact mode 0755"},
    {"id": "missing-permit", "class": "authority", "expect": "refusal before effect with request and lease reusable"},
    {"id": "missing-exclusive-mutation-lease", "class": "authority", "expect": "refusal before effect with request and permit reusable"},
    {"id": "not-yet-valid-permit", "class": "authority", "expect": "refusal before effect outside the lower validity bound"},
    {"id": "expired-permit", "class": "authority", "expect": "refusal before effect outside the upper validity bound"},
    {"id": "structurally-equal-request-substitution", "class": "authority", "expect": "pointer-distinct request seal cannot consume another request permit"},
    {"id": "mutation-lease-instance-substitution", "class": "authority", "expect": "permit and lease from different request or authority instances refuse before effect"},
    {"id": "nonce-reuse", "class": "authority", "expect": "one authority rejects reused nonce material"},
    {"id": "short-root-secret", "class": "authority", "expect": "authority construction rejects insufficient secret material without echoing it"},
    {"id": "short-permit-nonce", "class": "authority", "expect": "permit issuance rejects insufficient nonce material before reservation"},
    {"id": "invalid-validity-window", "class": "authority", "expect": "inverted and overlong validity windows reject before reservation"},
    {"id": "duplicate-semantic-request-issuance", "class": "authority", "expect": "one authority rejects duplicate request-instance reservation"},
    {"id": "concurrent-identical-contenders", "class": "race", "expect": "one atomic seal transition wins and every contender loses terminally"},
    {"id": "accepted-plan-digest-substitution", "class": "binding", "expect": "refusal before effect"},
    {"id": "canonical-plan-record-substitution", "class": "binding", "expect": "refusal before effect"},
    {"id": "plan-mutation-substitution", "class": "binding", "expect": "refusal before effect"},
    {"id": "desired-byte-substitution", "class": "binding", "expect": "refusal before effect"},
    {"id": "template-source-row-substitution", "class": "binding", "expect": "refusal before effect"},
    {"id": "template-manifest-substitution", "class": "binding", "expect": "refusal before effect"},
    {"id": "template-catalog-substitution", "class": "binding", "expect": "refusal before effect"},
    {"id": "desired-mode-substitution", "class": "binding", "expect": "refusal before effect"},
    {"id": "observed-mode-substitution", "class": "binding", "expect": "refusal before effect"},
    {"id": "case-aliased-plan-path", "class": "path", "expect": "refusal before effect"},
    {"id": "traversal-plan-path", "class": "path", "expect": "canonical path parser refuses before request preparation"},
    {"id": "same-content-target-rename-replacement", "class": "stale", "expect": "object identity drift refuses before effect even when bytes are unchanged"},
    {"id": "target-permission-drift", "class": "stale", "expect": "mode drift refuses before effect"},
    {"id": "protected-unowned-path-mutation", "class": "stale", "expect": "recursive protected-state drift refuses before effect"},
    {"id": "protected-managed-ancestor-descendant-preserved", "class": "positive", "expect": "an unrelated protected descendant below a managed strict ancestor is preserved while exact managed leaves apply"},
    {"id": "protected-managed-ancestor-descendant-drift", "class": "stale", "expect": "drift in an unrelated protected descendant below a managed strict ancestor refuses before effect"},
    {"id": "protected-symlink-refusal", "class": "object", "expect": "descriptor-relative protected traversal rejects a symlink before authority issuance"},
    {"id": "protected-hardlink-refusal", "class": "object", "expect": "descriptor-relative protected traversal rejects multiply linked regular files before authority issuance"},
    {"id": "protected-fifo-refusal", "class": "object", "expect": "descriptor-relative protected traversal rejects a FIFO without blocking"},
    {"id": "protected-socket-refusal", "class": "object", "expect": "descriptor-relative protected traversal rejects a socket without blocking"},
    {"id": "git-metadata-mutation", "class": "stale", "expect": "Git control-state drift refuses before effect"},
    {"id": "repository-root-replacement-before-effect", "class": "stale", "expect": "root object and descriptor binding drift refuse before effect"},
    {"id": "repository-root-replacement-after-effect", "class": "race", "expect": "started effect cannot claim rollback and terminates ambiguous"},
    {"id": "managed-ancestor-replacement-before-effect", "class": "stale", "expect": "unique strict target ancestor identity replacement refuses before effect"},
    {"id": "managed-shared-ancestor-replacement-before-effect", "class": "stale", "expect": "shared strict target ancestor identity replacement refuses before effect"},
    {"id": "managed-ancestor-mode-drift-before-effect", "class": "stale", "expect": "strict target ancestor mode drift refuses before effect"},
    {"id": "managed-ancestor-symlink-substitution-before-effect", "class": "object", "expect": "strict target ancestor symlink substitution refuses before effect"},
    {"id": "managed-ancestor-special-substitution-before-effect", "class": "object", "expect": "strict target ancestor special-object substitution refuses before effect"},
    {"id": "managed-ancestor-case-alias-before-effect", "class": "path", "expect": "strict target ancestor case alias refuses before effect"},
    {"id": "managed-ancestor-replacement-after-final-effect", "class": "race", "expect": "unique strict target ancestor replacement after postflight cannot cross the final green observation"},
    {"id": "managed-shared-ancestor-replacement-after-final-effect", "class": "race", "expect": "shared strict target ancestor replacement after postflight cannot cross the final green observation"},
    {"id": "managed-ancestor-mode-drift-after-final-effect", "class": "race", "expect": "strict target ancestor chmod after postflight cannot cross the final green observation"},
    {"id": "managed-ancestor-symlink-substitution-after-final-effect", "class": "race", "expect": "strict target ancestor symlink substitution after postflight cannot cross the final green observation"},
    {"id": "managed-ancestor-special-substitution-after-final-effect", "class": "race", "expect": "strict target ancestor special-object substitution after postflight cannot cross the final green observation"},
    {"id": "managed-ancestor-case-alias-after-final-effect", "class": "race", "expect": "strict target ancestor case alias after postflight cannot cross the final descriptor-relative observation"},
    {"id": "descriptor-parent-a-b-hybrid-during-capture", "class": "race", "expect": "a held parent descriptor, descendant-relative open, and final named-to-held recheck reject an ancestor A plus descendant B hybrid before authority issuance"},
    {"id": "descriptor-leaf-a-b-hybrid-during-capture", "class": "race", "expect": "a held leaf descriptor with duplicate reads and final named-to-held recheck rejects replacement during leaf capture"},
    {"id": "descriptor-missing-ancestor-publish-during-capture", "class": "race", "expect": "an exact-name missing boundary is held through the full capture and rejects concurrent publication before authority issuance"},
    {"id": "descriptor-shared-ancestor-swap-during-capture", "class": "race", "expect": "every target path reuses only descriptor-linked shared-ancestor identity and rejects cross-path A/B assembly"},
    {"id": "descriptor-case-alias-during-capture", "class": "path", "expect": "bounded parent-descriptor enumeration rejects a case alias before openat authorization"},
    {"id": "descriptor-postflight-within-capture-swap", "class": "race", "expect": "an ancestor swap inside the postflight descriptor capture cannot produce success or complete rollback"},
    {"id": "descriptor-final-within-capture-swap", "class": "race", "expect": "an ancestor swap inside the final green descriptor capture terminates ambiguous and cannot produce success"},
    {"id": "target-two-leaf-alternating-hybrid", "class": "false-pass", "expect": "two equal ordinary row sets assembled from alternating leaves are rejected because complete target collects bind Darwin ctime and require one common stable instant"},
    {"id": "target-same-inode-mutate-restore-aba", "class": "false-pass", "expect": "same-inode same-length target content ABA is rejected even when payload, mode, and length are restored between complete collects"},
    {"id": "target-content-complete-collect-race", "class": "race", "expect": "same-length target content drift between complete collects refuses authority issuance"},
    {"id": "target-length-complete-collect-race", "class": "race", "expect": "target length drift between complete collects refuses authority issuance"},
    {"id": "target-mode-complete-collect-race", "class": "race", "expect": "target mode drift between complete collects refuses authority issuance"},
    {"id": "target-ownership-complete-collect-race", "class": "race", "expect": "target ownership drift between complete collects refuses authority issuance"},
    {"id": "target-link-complete-collect-race", "class": "race", "expect": "target link-count drift between complete collects refuses authority issuance"},
    {"id": "target-special-complete-collect-race", "class": "race", "expect": "target special-object substitution between complete collects refuses authority issuance without blocking"},
    {"id": "target-path-complete-collect-race", "class": "race", "expect": "target exact-path and case-alias drift between complete collects refuses authority issuance"},
    {"id": "target-final-green-post-protected-aba", "class": "false-pass", "expect": "a target-only ABA after the protected-after collect begins is caught by the complete target recheck before settlement"},
    {"id": "target-rollback-attribution-aba", "class": "rollback", "expect": "unmediated target ABA after a scoped effect destroys attribution and withholds complete rollback even when semantic bytes look allowed"},
    {"id": "target-reconciliation-authorized-snapshot-gaps", "class": "rollback", "expect": "every reconciliation target collect must exactly equal the retained ctime-bound authorized target before semantic rollback equivalence; target ABA before the first collect, between collects, or after protected-after terminates ambiguous"},
    {"id": "protected-permit-issuance-a-b-swap", "class": "race", "expect": "enumeration-relative inode binding rejects a protected A/B swap during permit issuance without starting an effect"},
    {"id": "protected-late-same-inode-write-issuance", "class": "race", "expect": "a late same-inode protected write after its regular row was read is detected by the complete versioned recheck; the exact write remains observable and no authority issues"},
    {"id": "protected-two-file-hybrid-issuance", "class": "race", "expect": "coordinated writes after the first regular row cannot assemble a two-file hybrid protected snapshot or issue authority"},
    {"id": "protected-mutate-restore-aba", "class": "race", "expect": "Darwin ctime seconds and nanoseconds reject same-inode content ABA even when the original payload and length are restored before the final recheck"},
    {"id": "protected-pre-effect-a-b-swap", "class": "race", "expect": "a held protected directory and named-to-held recheck reject an A/B swap during immediate pre-effect validation"},
    {"id": "protected-pre-effect-late-length-write", "class": "race", "expect": "a protected length-changing write after row revalidation refuses before effect and remains exactly observable"},
    {"id": "protected-postflight-hidden-write-a-b-swap", "class": "false-pass", "expect": "a clean B directory cannot hide an undeclared write in held directory A during postflight capture"},
    {"id": "protected-postflight-late-mode-change", "class": "false-pass", "expect": "a late same-inode chmod after a postflight protected row is read cannot produce success or complete rollback"},
    {"id": "protected-final-green-a-b-swap", "class": "race", "expect": "a protected A/B swap during final green capture terminates ambiguous and cannot produce success"},
    {"id": "protected-final-green-recheck-late-write", "class": "false-pass", "expect": "the FinalGreen recheck after target capture detects a late protected write overlapping the target linearization point and terminates ambiguous"},
    {"id": "protected-rollback-a-b-swap", "class": "rollback", "expect": "a protected A/B swap at rollback validation withholds complete rollback and terminates ambiguous"},
    {"id": "protected-rollback-late-write", "class": "rollback", "expect": "a late protected regular-file write during the non-green rollback gate withholds rollback and terminates ambiguous"},
    {"id": "protected-reconciliation-a-b-swap", "class": "rollback", "expect": "a protected A/B swap during ambiguity reconciliation cannot fabricate exact prior state"},
    {"id": "protected-reconciliation-late-write", "class": "rollback", "expect": "a late protected write during bracketed reconciliation prevents a rolled-back terminal claim and remains exactly observable"},
    {"id": "target-symlink-substitution", "class": "object", "expect": "no-follow preflight refusal"},
    {"id": "target-hardlink-substitution", "class": "object", "expect": "multi-link preflight refusal"},
    {"id": "target-fifo-substitution", "class": "object", "expect": "nonblocking special-object refusal"},
    {"id": "target-socket-substitution", "class": "object", "expect": "nonblocking special-object refusal"},
    {"id": "target-cross-device-substitution", "class": "object", "expect": "same-device guard refusal"},
    {"id": "apply-failure-complete-rollback", "class": "rollback", "expect": "exact prior bytes, namespace, metadata, protected state, Git state, and context are proved before rolled-back terminal status"},
    {"id": "mode-failure-complete-rollback", "class": "rollback", "expect": "exact prior Unix mode and bytes are restored while mediator-owned ctime advancement remains attributable because authorized_target records the post-rollback snapshot"},
    {"id": "rollback-failure-ambiguous", "class": "rollback", "expect": "no reusable authority and no success projection"},
    {"id": "undeclared-write-ambiguous", "class": "scope", "expect": "protected-tree mutation prevents success and exact rollback claim"},
    {"id": "unowned-overwrite-conflict", "class": "scope", "expect": "conflict prevents request preparation and permit issuance"},
    {"id": "out-of-plan-effect-request", "class": "scope", "expect": "scoped effect wrapper rejects every compare-exchange outside exact forward or rollback rows"},
    {"id": "green-effect-without-write", "class": "false-pass", "expect": "post-effect target verification prevents fabricated success"},
    {"id": "verify-as-apply", "class": "false-pass", "expect": "read-only verification neither consumes nor substitutes for apply authority"},
    {"id": "permit-debug-redaction", "class": "secret", "expect": "permit Debug contains no secret, nonce, binding, or authority material"},
    {"id": "content-free-diagnostics", "class": "secret", "expect": "typed failures expose fixed causes only and never echo repository bytes"},
    {"id": "move-only-permit-and-request", "class": "type", "expect": "request, permit, and lease implement neither Clone, Copy, Serialize, nor Deserialize as authority carriers"},
    {"id": "private-success-construction", "class": "type", "expect": "serialized success can only be constructed after all terminal checks"},
    {"id": "production-permit-issuer-absent", "class": "ceiling", "expect": "test-only issuer does not activate root authority"},
    {"id": "public-apply-dispatch-absent", "class": "ceiling", "expect": "inspect, plan, and verify remain distinct from apply execution"},
    {"id": "non-apple-fail-closed", "class": "host", "expect": "unsupported descriptor-bound target cannot reach an effect"},
    {"id": "no-shell-network-or-git-mutation-authority", "class": "effect", "expect": "mediator invokes only the accepted repository-fit kernel and bounded local observations"}
  ],
  "no_claim_statement": "This fixture catalog describes isolated source tests only. It does not activate root issuance, public apply dispatch, installed behavior, a live user repository mutation, readiness, release, or completion."
}
