{
  "schema": "harness-ultragoal.red-packet.v1",
  "id": "orchestrator-state-machine-observability-events-red",
  "expected_failure": {
    "check_id": "source-obligation-coverage",
    "error": "mandatory_law_specific_guard_not_enforced:orchestrator-state-machine:observability_events"
  },
  "base_fixture_path": "fixtures/mandatory-law-surfaces/valid/orchestrator-state-machine.json",
  "json_patch": [
    {
      "op": "replace",
      "path": "/law_specific/observability_events",
      "value": false
    }
  ],
  "notes": "orchestrator-state-machine must fail when observability_events is not enforced.",
  "materialization": {
    "expected_validation_layer": "package",
    "post_patch_schema_valid": true,
    "first_failure_must_match_expected": true
  },
  "preconditions": [
    {
      "path": "/law_specific/observability_events",
      "exists": true
    }
  ],
  "postconditions": [
    {
      "path": "/law_specific/observability_events",
      "expectation": "mandatory_law_specific_guard_not_enforced:orchestrator-state-machine:observability_events"
    }
  ]
}
