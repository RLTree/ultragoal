{
  "schema": "harness-ultragoal.red-packet.v1",
  "id": "config-precedence-defaults-env-indirection-unknown-config-accepted-red",
  "expected_failure": {
    "check_id": "source-obligation-coverage",
    "error": "mandatory_law_specific_guard_not_enforced:config-precedence-defaults-env-indirection:unknown_config_accepted"
  },
  "base_fixture_path": "fixtures/mandatory-law-surfaces/valid/config-precedence-defaults-env-indirection.json",
  "json_patch": [
    {
      "op": "replace",
      "path": "/law_specific/unknown_config_accepted",
      "value": false
    }
  ],
  "notes": "config-precedence-defaults-env-indirection must fail when unknown_config_accepted is not enforced.",
  "materialization": {
    "expected_validation_layer": "package",
    "post_patch_schema_valid": true,
    "first_failure_must_match_expected": true
  },
  "preconditions": [
    {
      "path": "/law_specific/unknown_config_accepted",
      "exists": true
    }
  ],
  "postconditions": [
    {
      "path": "/law_specific/unknown_config_accepted",
      "expectation": "mandatory_law_specific_guard_not_enforced:config-precedence-defaults-env-indirection:unknown_config_accepted"
    }
  ]
}
