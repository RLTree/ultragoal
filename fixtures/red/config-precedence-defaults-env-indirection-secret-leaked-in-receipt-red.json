{
  "schema": "harness-ultragoal.red-packet.v1",
  "id": "config-precedence-defaults-env-indirection-secret-leaked-in-receipt-red",
  "expected_failure": {
    "check_id": "source-obligation-coverage",
    "error": "mandatory_law_specific_guard_not_enforced:config-precedence-defaults-env-indirection:secret_leaked_in_receipt"
  },
  "base_fixture_path": "fixtures/mandatory-law-surfaces/valid/config-precedence-defaults-env-indirection.json",
  "json_patch": [
    {
      "op": "replace",
      "path": "/law_specific/secret_leaked_in_receipt",
      "value": false
    }
  ],
  "notes": "config-precedence-defaults-env-indirection must fail when secret_leaked_in_receipt is not enforced.",
  "materialization": {
    "expected_validation_layer": "package",
    "post_patch_schema_valid": true,
    "first_failure_must_match_expected": true
  },
  "preconditions": [
    {
      "path": "/law_specific/secret_leaked_in_receipt",
      "exists": true
    }
  ],
  "postconditions": [
    {
      "path": "/law_specific/secret_leaked_in_receipt",
      "expectation": "mandatory_law_specific_guard_not_enforced:config-precedence-defaults-env-indirection:secret_leaked_in_receipt"
    }
  ]
}
