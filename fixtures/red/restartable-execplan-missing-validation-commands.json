{
  "schema": "harness-ultragoal.red-packet.v1",
  "id": "restartable-execplan-missing-validation-commands",
  "expected_failure": {
    "check_id": "source-obligation-coverage",
    "error": "restartable_execplan_missing_section:validation_commands"
  },
  "base_fixture_path": "fixtures/law-surfaces/valid/restartable-execplan-receipt.json",
  "json_patch": [
    {
      "op": "replace",
      "path": "/required_sections/validation_commands",
      "value": false
    }
  ],
  "notes": "ExecPlan receipt fails when validation commands are missing.",
  "materialization": {
    "post_patch_schema_valid": true,
    "expected_validation_layer": "package",
    "first_failure_must_match_expected": false
  },
  "preconditions": [
    {
      "path": "fixtures/law-surfaces/valid/restartable-execplan-receipt.json",
      "exists": true
    }
  ],
  "postconditions": [
    {
      "path": "/required_sections/validation_commands",
      "expectation": "restartable_execplan_missing_section:validation_commands"
    }
  ]
}
