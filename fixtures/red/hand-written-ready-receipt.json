{
  "schema": "harness-ultragoal.red-packet.v1",
  "id": "hand-written-ready-receipt",
  "expected_failure": {
    "check_id": "ready-receipt-provenance",
    "error": "ready_receipt_not_validator_output"
  },
  "base_fixture_path": "fixtures/valid/minimal-goal-run.json",
  "json_patch": [
    {
      "op": "replace",
      "path": "/ready_for_merge/provenance/validator_run_id",
      "value": "different-run-id"
    }
  ],
  "notes": "replace ready_for_merge.generated_by with human-authored value",
  "materialization": {
    "expected_validation_layer": "semantic",
    "post_patch_schema_valid": true,
    "first_failure_must_match_expected": true
  },
  "preconditions": [
    {
      "path": "/ready_for_merge/schema",
      "exists": true
    }
  ],
  "postconditions": [
    {
      "path": "/ready_for_merge/provenance/validator_run_id",
      "expectation": "ready_receipt_not_validator_output"
    }
  ]
}
