{
  "schema": "harness-ultragoal.red-packet.v1",
  "id": "config-precedence-defaults-env-indirection-default-overriding-repo-policy-red",
  "expected_failure": {
    "check_id": "source-obligation-coverage",
    "error": "mandatory_law_specific_guard_not_enforced:config-precedence-defaults-env-indirection:default_overriding_repo_policy"
  },
  "base_fixture_path": "fixtures/mandatory-law-surfaces/valid/config-precedence-defaults-env-indirection.json",
  "json_patch": [
    {
      "op": "replace",
      "path": "/law_specific/default_overriding_repo_policy",
      "value": false
    }
  ],
  "notes": "config-precedence-defaults-env-indirection must fail when default_overriding_repo_policy is not enforced.",
  "materialization": {
    "expected_validation_layer": "package",
    "post_patch_schema_valid": true,
    "first_failure_must_match_expected": true
  },
  "preconditions": [
    {
      "path": "/law_specific/default_overriding_repo_policy",
      "exists": true
    }
  ],
  "postconditions": [
    {
      "path": "/law_specific/default_overriding_repo_policy",
      "expectation": "mandatory_law_specific_guard_not_enforced:config-precedence-defaults-env-indirection:default_overriding_repo_policy"
    }
  ]
}
