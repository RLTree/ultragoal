{
  "schema": "harness-ultragoal.red-packet.v1",
  "id": "clean-checkout-missing-root-check",
  "expected_failure": {
    "check_id": "source-obligation-coverage",
    "error": "clean_checkout_missing_root_check"
  },
  "base_fixture_path": "fixtures/law-surfaces/valid/clean-checkout-command-discovery-receipt.json",
  "json_patch": [
    {
      "op": "remove",
      "path": "/root_check_command"
    }
  ],
  "notes": "Command discovery fails without a root check command.",
  "materialization": {
    "post_patch_schema_valid": true,
    "expected_validation_layer": "package",
    "first_failure_must_match_expected": false
  },
  "preconditions": [
    {
      "path": "fixtures/law-surfaces/valid/clean-checkout-command-discovery-receipt.json",
      "exists": true
    }
  ],
  "postconditions": [
    {
      "path": "/root_check_command",
      "expectation": "clean_checkout_missing_root_check"
    }
  ]
}
