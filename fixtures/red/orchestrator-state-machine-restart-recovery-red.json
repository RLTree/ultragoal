{
  "schema": "harness-ultragoal.red-packet.v1",
  "id": "orchestrator-state-machine-restart-recovery-red",
  "expected_failure": {
    "check_id": "source-obligation-coverage",
    "error": "mandatory_law_specific_guard_not_enforced:orchestrator-state-machine:restart_recovery"
  },
  "base_fixture_path": "fixtures/mandatory-law-surfaces/valid/orchestrator-state-machine.json",
  "json_patch": [
    {
      "op": "replace",
      "path": "/law_specific/restart_recovery",
      "value": false
    }
  ],
  "notes": "orchestrator-state-machine must fail when restart_recovery is not enforced.",
  "materialization": {
    "expected_validation_layer": "package",
    "post_patch_schema_valid": true,
    "first_failure_must_match_expected": true
  },
  "preconditions": [
    {
      "path": "/law_specific/restart_recovery",
      "exists": true
    }
  ],
  "postconditions": [
    {
      "path": "/law_specific/restart_recovery",
      "expectation": "mandatory_law_specific_guard_not_enforced:orchestrator-state-machine:restart_recovery"
    }
  ]
}
