{
  "schema": "harness-ultragoal.red-packet.v1",
  "id": "schema-plugin-manifest-name-const",
  "expected_failure": {
    "check_id": "schema-valid",
    "error": "schema_validation_failed"
  },
  "base_fixture_path": "fixtures/valid/minimal-goal-run.json",
  "json_patch": [
    {
      "op": "replace",
      "path": "/plugin_manifest/name",
      "value": "not-harness-ultragoal"
    }
  ],
  "notes": "Rust schema validation must enforce const constraints from package schemas",
  "materialization": {
    "expected_validation_layer": "schema",
    "post_patch_schema_valid": false,
    "first_failure_must_match_expected": true
  },
  "preconditions": [
    {
      "path": "/plugin_manifest/name",
      "exists": true
    }
  ],
  "postconditions": [
    {
      "path": "/plugin_manifest/name",
      "expectation": "plugin manifest name const is enforced"
    }
  ]
}
