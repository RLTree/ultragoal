{
  "schema": "harness-ultragoal.red-packet.v1",
  "materialization": {
    "expected_validation_layer": "schema",
    "post_patch_schema_valid": false,
    "first_failure_must_match_expected": true
  },
  "preconditions": [
    {
      "path": "/schema",
      "exists": true
    }
  ],
  "postconditions": [
    {
      "path": "/expected_failure/error",
      "expectation": "actor_validation_timestamp_malformed remains the first failing error"
    }
  ],
  "id": "malformed-actor-validation-range-timestamp",
  "expected_failure": {
    "check_id": "schema-valid",
    "error": "actor_validation_timestamp_malformed"
  },
  "base_fixture_path": "fixtures/valid/minimal-goal-run.json",
  "json_patch": [
    {
      "op": "replace",
      "path": "/lane_registry/lanes/0/actor_binding/validated_at",
      "value": "2026-13-16T00:00:00Z"
    }
  ],
  "notes": "out-of-range date-time fields must fail closed instead of normalizing"
}
