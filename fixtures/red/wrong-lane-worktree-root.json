{
  "schema": "harness-ultragoal.red-packet.v1",
  "id": "wrong-lane-worktree-root",
  "expected_failure": {
    "check_id": "worktree-teardown",
    "error": "wrong_lane_worktree_root"
  },
  "base_fixture_path": "fixtures/valid/minimal-goal-run.json",
  "json_patch": [
    {
      "op": "replace",
      "path": "/lane_registry/lanes/0/workspace_status/observed_workspace",
      "value": "/wrong/repo"
    }
  ],
  "materialization": {
    "expected_validation_layer": "semantic",
    "post_patch_schema_valid": true,
    "first_failure_must_match_expected": true
  },
  "preconditions": [
    {
      "path": "/schema",
      "exists": true
    }
  ],
  "postconditions": [
    {
      "path": "/lane_registry/lanes/0/workspace_status/observed_workspace",
      "expectation": "observed worktree root must match lane workspace"
    }
  ],
  "notes": "observed worktree root must match lane workspace"
}
