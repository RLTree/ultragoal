{
  "base_fixture_path": "fixtures/valid/minimal-goal-run.json",
  "expected_failure": {
    "check_id": "lane-scope-overlap",
    "error": "invalid_lane_resource_path"
  },
  "id": "lane-temp-root-dot-segment",
  "json_patch": [
    {
      "op": "replace",
      "path": "/lane_registry/lanes/0/scratch_roots/0",
      "value": ".scratch/alt/../fixture"
    }
  ],
  "materialization": {
    "expected_validation_layer": "semantic",
    "first_failure_must_match_expected": true,
    "post_patch_schema_valid": true
  },
  "notes": "scratch_roots must reject dot-segment aliases",
  "postconditions": [
    {
      "expectation": "invalid_lane_resource_path",
      "path": "/lane_registry/lanes/0"
    }
  ],
  "preconditions": [
    {
      "exists": true,
      "path": "/schema"
    }
  ],
  "schema": "harness-ultragoal.red-packet.v1"
}
