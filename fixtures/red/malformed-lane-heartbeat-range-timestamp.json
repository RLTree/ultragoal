{
  "schema": "harness-ultragoal.red-packet.v1",
  "materialization": {
    "expected_validation_layer": "schema",
    "post_patch_schema_valid": false,
    "first_failure_must_match_expected": true
  },
  "preconditions": [
    {
      "path": "/schema",
      "exists": true
    }
  ],
  "postconditions": [
    {
      "path": "/expected_failure/error",
      "expectation": "lane_heartbeat_timestamp_malformed remains the first failing error"
    }
  ],
  "id": "malformed-lane-heartbeat-range-timestamp",
  "expected_failure": {
    "check_id": "schema-valid",
    "error": "lane_heartbeat_timestamp_malformed"
  },
  "base_fixture_path": "fixtures/valid/minimal-goal-run.json",
  "json_patch": [
    {
      "op": "replace",
      "path": "/lane_registry/lanes/0/last_heartbeat",
      "value": "2026-06-16T25:00:00Z"
    }
  ],
  "notes": "out-of-range heartbeat timestamps must fail closed instead of normalizing"
}
